---
parent: Globalconfig
ancestor: Rules
permalink: /rules/Proj4026
---

# Proj4026: Specify is_global
Global configuration files must explicitly define the `is_global` property.
For files not named `.globalconfig`, this property is technically required
for the configuration to work properly. For files named `.globalconfig`,
explicitly setting `is_global = true` is [highly recommended](https://learn.microsoft.com/dotnet/fundamentals/code-analysis/configuration-files#naming)
for clarity and consistency.

## Non-compliant
``` ini
is_global = false
        
dotnet_diagnostic.CA1860.severity = none
```

or

``` ini
dotnet_diagnostic.CA1860.severity = none
```


## Compliant
``` ini
is_global = true
        
dotnet_diagnostic.CA1860.severity = none
```
