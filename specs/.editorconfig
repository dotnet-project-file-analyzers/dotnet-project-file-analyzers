# Roslyn Analyzers
[*.cs]

dotnet_diagnostic.CA1861.severity = none    #  PPrefer 'static readonly' fields over constant array arguments if the called method is called repeatedly and is not mutating the passed array
