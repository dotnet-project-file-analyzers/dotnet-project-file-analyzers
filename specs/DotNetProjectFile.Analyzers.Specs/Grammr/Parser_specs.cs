namespace Specs.Parser_specs;

public class Parses
{
    //[TestCase("\t grammr =\tsyntax \t\r\n")]
    //[TestCase("grammr=syntax")]
    //[TestCase("grammr=syntax;")]
    //[TestCase("grammr=syntax # with comment")]
    //public void lines(string text)
    //{
    //	var reader = new SourceReader(text);

    //	var kvp = DotNetProjectFile.Ini.IniGrammar.kvp.Parse(ref reader);

    //	kvp.Value.Should().Be(KeyValuePair.Create("grammr", "syntax"));

    //	reader.EOS.Should().BeTrue();
    //}
}
